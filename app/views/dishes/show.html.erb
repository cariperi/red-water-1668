<h1>Dish Show Page</h1>

<h3>Dish Name: <%= @dish.name %></h3>
<h3>Dish Description: <%= @dish.description %></h3>

<h2>Ingredients for this Dish:</h2>

<% @dish.ingredients.each do |ingredient| %>
  <ul>
  <div id="ingredient-<%= ingredient.id %>">
    <li><h3>Ingredient Name: <%= ingredient.name %></h3></li>
  </div>
  </ul>
<% end %>

<h3>Total Calories: <%= @dish.get_calorie_count %></h3>
<h3>Chef Name: <%= @dish.chef_name %></h3>


 <div id="add-ingredient">

  <h2>Add an Ingredient</h2>

  <%= form_with url: ingredient_dishes_path, local: true, method: :post do |form| %>
    <%= form.label :ingredient_id, "Enter Ingredient ID" %>
    <%= form.number_field :ingredient_id %>
    <%= form.hidden_field :dish_id, value: @dish.id %>
    <%= form.submit "Submit" %>
  <% end %>
 </div>
